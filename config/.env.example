# AI Test Pilot — environment variables. Copy to config/.env and fill in.
# Secrets ONLY here — never hardcode, never commit config/.env.

# Any OpenAI-compatible LLM gateway (scenario generation + failure triage)
LLM_BASE_URL=https://your-llm-gateway/v1
LLM_MODEL=your-default-model
LLM_API_KEY=sk-...
