# AI Test Pilot — runtime dependencies (Python 3.10+)
pydantic==2.10.1
pydantic-settings==2.13.1
jinja2==3.1.4
duckdb==1.5.2

# LLM gateway client (OpenAI-compatible; scenario generation + triage)
langchain-openai==1.1.10
langchain-core==1.2.17
python-dotenv==0.21.0

# python_pytest adapter target runner + this project's own tests
pytest==7.4.4

# MCP server (FastMCP)
mcp==1.28.0

# web_playwright adapter (run `python -m playwright install chromium` after install)
playwright==1.60.0
