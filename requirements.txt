# AI Test Pilot — runtime dependencies (Python 3.10+).
# Single source of truth is config/requirements.txt; this root file re-exports it so that
# `pip install -r requirements.txt` works from the repo root (and stays in sync automatically).
-r config/requirements.txt
